# Copy to .env. Expo inlines EXPO_PUBLIC_* variables at build time.

# Where the API lives, including the /api path. Leave unset to use the
# production API, which is the built-in fallback.
EXPO_PUBLIC_API_URL=http://localhost:5000/api
